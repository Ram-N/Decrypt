About Decrypt:

Just an attempt to solve a few cryptic clues using Python and NLTK

On Starting a Git Bash Window:

cd to your Git repository (source files)

$ ssh -T git@github.com
#if it says Hi Ram! you are okay

2. Set up your INFO Next establish who you are to git:
  git config --global user.name "Ram Narasimhan"
  git config --global user.email ramnarasimhan@gmail.com

3. Set your GitHub Token
  git config --global github.user Ram-N
  git config --global github.token 8a4523dbca2c3f6391684661ea027867

Now you can push files - add commit push
Git commands:
http://help.github.com/create-a-repo/












cd Decrypt # Directory name same as the GitHub repo name
git init
git remote add origin git@github.com:Ram-N/Decrypt.git
git add *.py # all the new files that you want to push to Github
git commit -m 'some comment on what is being pushed to remote'
git push -u origin master


=========================================================
http://help.github.com/win-set-up-git/

Global setup:

 Set up git
  git config --global user.name "Ram Narasimhan"
  git config --global user.email ramnarasimhan@gmail.com



Next steps:

  mkdir IRT-Prediction
  cd IRT-Prediction
  git init
  touch README
  git add README
  git commit -m 'first commit'
  git remote add origin git@github.com:Ram-N/IRT-Prediction.git
  git push -u origin master


Existing Git Repo?

  cd existing_git_repo
  git remote add origin git@github.com:Ram-N/IRT-Prediction.git
  git push -u origin master


Importing a Subversion Repo?

  Click here


When you're done:

  Continue


